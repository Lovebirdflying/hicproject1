var checkArray = [1, 2, 3];

let outcome = Array.isArray(checkArray)

//console.log( checkArray && checkArray.length ? true : false)

console.log("18. The Bolean Value of an object in an array:", outcome)